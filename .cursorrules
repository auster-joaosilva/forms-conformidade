# shadcn instructions

Add components with the shadcn CLI (this project uses npm):

```bash
npx shadcn@latest add button
```

Keep the `@/` alias in components.json — the CLI fails with the `#/` alias.
